services:
  worker:
    build:
      context: .
      dockerfile: Dockerfile.app
    restart: unless-stopped
    env_file:
      - .env.laptop
    environment:
      BM_DATABASE_URL: ${BM_DATABASE_URL}
      BM_ENABLE_SCHEDULER: "false"
      BM_LOG_LEVEL: ${BM_LOG_LEVEL:-INFO}
    command: ["sh", "/app/docker/worker-entrypoint.sh"]
